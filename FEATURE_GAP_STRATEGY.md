# استراتيجية سد الفجوات — LENA

> تاريخ التحليل: 2026-08-20 — الفرع `arena/01a01f12-lenadress` من `main @ d7dd78f` (PR #144).
> أعدّ هذه الوثيقة وكيل Arena بصفة محلل منتجات مستقل، بعد فحص الكود والوثائق والاختبارات (698/698 خضراء)
> ومقارنة بأنماط منتجات مماثلة موثقة (Booqable, EZRentOut, Current RMS, CloudBridal, TSE Bridal, StyleSavvy).
> القاعدة: كل ادعاء هنا مربوط بدليل من المستودع أو بمصدر مسمّى، وكل قدرة مقترحة لها أصغر نسخة ومعايير قبول وقياس نجاح.

## 1. تعريف المنتج والنضج (مستنتج من الأدلة)

| البُعد | الاستنتاج | الدليل |
| --- | --- | --- |
| الفئة | نظام تشغيل معرض تأجير/بيع فساتين مناسبات (rental + POS تشغيلي)، لا SaaS | `README.md`, ADR 0001 |
| السوق/المستخدمون | معرض واحد في عُمان؛ مالكة (`admin`) وموظفات كاونتر (`staff`)؛ العميلات عبر WhatsApp وصفحة عامة | `PROJECT_OVERVIEW.md` §2 |
| النتيجة الأساسية | يوم تشغيل كامل بلا أخطاء: حجز بلا تعارض ← تحصيل صحيح ← تسليم/استرجاع موثق ← خدمة ← إقفال مضبوط ← تقارير | `docs/BUSINESS_MODEL.md`, 698 اختبارًا |
| دورة الحياة | قطعة: إدخال ← متاحة ← محجوزة/مؤجرة ← فحص/غسيل/صيانة ← متاحة؛ أو مباعة/مؤرشفة. عميلة: جديدة ← عائدة ← سلوك (trusted/warning/blocked) | PROJECT_OVERVIEW §3–5 |
| النموذج التجاري للبرنامج | ترخيص/تسليم لمعرض واحد؛ لا اشتراكات متعددة المستأجرين؛ لا دفع إلكتروني (قرار منتج مؤكد) | README «نطاق المنتج», ADR 0001 |
| النضج | Feature-complete لمرحلة v1.0 تقريبًا؛ البوابة المتبقية أدلة أجهزة حقيقية (`5.06` BLOCKED) وليست قدرات | `EXECUTION_CHECKLIST.md` Phases 0–13 |

**حكم المحلل:** المنتج ليس ناقص المزايا التشغيلية؛ فجواته الحقيقية في **الثقة والاستمرارية** أولًا، ثم في إثبات التشغيل الميداني. أي ميزة جديدة واجهة-أولى الآن ستكون تضخيمًا مبكرًا (overbuilding).

## 2. فجوة تمنع النتيجة الأساسية

**لا توجد قدرة غائبة تمنع النتيجة الأساسية** حسب الدليل: دورة التأجير/البيع كاملة ومغطاة باختبارات ذرية ومالية وتراجع.
ما يبدو كمانع هو في الحقيقة بندان من فئات أخرى:

1. **استمرارية البيانات (T1 أدناه):** لا تمنع التشغيل اليومي لكنها تحول أي خطأ بشري واحد (استيراد خاطئ، تصفير، فقدان جهاز قبل رفع آخر commit) إلى خسارة دائمة. مصنفة ثقة/استعادة لأنها لا تظهر إلا يوم الكارثة — وهي أعلى فجوة في هذه الوثيقة.
2. **أدلة الأجهزة الحقيقية (4.02/4.04/4.05 + 5.06):** الكاميرا والطباعة وتثبيت PWA رحلات معلّن عنها بلا إثبات ميداني. لا تُبنى بالكود؛ تحتاج جلسة جهاز مع المالكة (انظر «الآن/التالي»).

## 3. فجوات الثقة/الأمان/الاستعادة

### T1 — نسخ احتياطية لحظية على الخادم (Cloud point-in-time backups)
- **الدليل:** bucket `backups` منشأ بسياسات RLS كاملة في `supabase/migrations/0011` (خاص، 100MB، json/gzip، وصول المستخدمات النشطات) و**لا يستخدمه أي سطر في `src`** (grep شامل). النسخ الحالية تُنزَّل إلى الجهاز فقط (`backupExport.service.ts`). الخادم يحفظ اللقطة الحالية فقط (`showroom_state` revision) بلا أي تاريخ نقاط زمنية. `PROJECT_STATUS.md` صنّفها الخطر #1.
- **المتأثرون:** المالكة (خطر فقدان كل شيء)، الموظفات (استيراد خاطئ)، العميلات (سجلاتهن المالية).
- **المبرر المجالسي:** كل منتج سحابي مماثل (Booqable/CloudBridal/Current RMS) يقدّم نسخًا تلقائية محفوظة زمنيًا كوعد أساسي؛ الاكتفاء بملف على جهاز الكاونتر دون معيار 2026.
- **القيمة:** استعادة نقطة زمنية بعد خطأ بشري أو كارثة جهاز؛ إزالة أكبر خطر موثق.
- **التبعيات:** لا شيء جديد — البنية جاهزة (bucket + سياسات + عميل Supabase + نمط رفع موجود `supabaseImageUpload.ts`).
- **المخاطر:** منخفضة — إضافة بحتة، أفضل-جهد (best-effort)، لا تلمس مسار الإقفال؛ الاستعادة تعيد استخدام مسار الاستيراد المتحقق مع التراجع التلقائي.
- **التكلفة الجارية:** ~صفر ضمن خطة Supabase الحالية (20 نسخة × ~1–3MB). يجب مراقبة الحجم سنويًا.
- **أصغر نسخة:** رفع نسخة بعد كل تصدير (يدوي + بعد الإقفال) + احتفاظ بآخر 20 نسخة + بطاقة «نسخ الخادم» في الإعدادات (عرض/تنزيل/استعادة).
- **معايير القبول:** فشل الرفع لا يفشل التصدير أو الإقفال؛ الأسماء حتمية ومرتبة زمنيًا؛ الاستعادة تمر بـ`importDatabaseBackupCommand` نفسه بعد تأكيد صريح؛ اختبارات سلوكية بواجهة تخزين مزيفة + العقدة البرمجية للربط.
- **قياس النجاح:** % إقفالات لها نسخة خادم حديثة (تُرى من القائمة)؛ تمرين استعادة شهري ناجح؛ صفر حالات «النسخة الوحيدة كانت على الجهاز المفقود».

### T2 — حارس حجم اللقطة المركزية (Snapshot size guardrail)
- **الدليل:** كل command يعيد كتابة اللقطة كاملة عبر RPC بحد 20 MiB؛ صور الحالة تُخزن data URLs داخل السجلات (`ConditionPhotoCapture.tsx` سطر 88) رغم وجود bucket `condition-photos` جاهز. `PROJECT_STATUS.md` صنّفها الخطر #3 (stop-the-line).
- **المتأثرون:** الجميع عند بلوغ الحد — يتوقف الحفظ كليًا.
- **القيمة:** إنذار مبكر قبل توقف الحفظ؛ تمديد عمر التصميم الحالي سنوات.
- **أصغر نسخة:** قياس حجم اللقطة بعد كل commit وعرضه في الإعدادات مع تحذير 70%/85% + تسجيل `client_error_events`. (نقل صور الحالة إلى Storage خطوة تالية مستقلة — ترحيل بيانات يحتاج قرارًا منفصلًا.)
- **معايير القبول:** التحذير يظهر قبل النفاد بمسافة كافية؛ لا تغيير في سلوك الحفظ؛ اختبار محاكاة حجم.
- **قياس النجاح:** لا توجد حالة «رفض RPC بسبب الحجم» بلا إنذار سابق.
- **القرار:** NEXT — لا يُنفذ الآن حتى تستقر شريحة T1 (تتشاركان نفس الملفات).

### T3 — إدارة دورة الحسابات (provisioning/إلغاء/إعادة تعيين)
- **الدليل:** الواجهة الحالية (`AccountManagement.tsx`) تدير الدور والتفعيل فقط؛ إنشاء حساب جديد أو إعادة كلمة المرور يتطلب Supabase Dashboard؛ لا MFA policy موثقة (`PROJECT_STATUS.md` §6-المخاطر).
- **المتأثرون:** المالكة عند توظيف/مغادرة موظفة؛ أمن الحساب عالي الأثر.
- **أصغر نسخة:** تشغيلًا أولًا: دليل مصور في `docs/OPERATIONS_GUIDE.md` (إنشاء/تعطيل/إعادة تعيين من لوحة Supabase + تفعيل MFA للمالكة). برمجيًا لاحقًا: Edge Function بدعوات — يحتاج مفتاح service (قرار مالك).
- **معايير القبول:** المالكة تنجز إضافة موظفة من الدليل دون مساعدة تقنية؛ MFA موثق لحساب admin.
- **قياس النجاح:** زمن إضافة موظفة جديدة < 10 دقائق دون تدخل خارجي.
- **القرار:** NEXT (الدليل) / LATER (الأتمتة البرمجية).

### T4 — احتفاظ وحدود للسجلات المتراكمة (`showroom_mutations`, `client_error_events`)
- **الدليل:** append دائم بلا retention job في المستودع (`PROJECT_STATUS.md` §4).
- **أصغر نسخة:** استعلام دوري موثق + حذف يدوي مجدول ربع سنوي من SQL editor (أرشفة اختيارية). أتمتة pg_cron لاحقًا.
- **القرار:** LATER — لا يستحق بناء واجهة؛ حجم الفعلي غير مُقاس بعد، وقياسه أولًا جزء من T2.

### T5 — حدود محاولات PIN على الجهاز
- **الدليل:** لا rate limit/lockout موثق لـ PIN السداسي (`PROJECT_STATUS.md` §6).
- **أصغر نسخة:** تأخير تصاعدي محلي بعد 5 محاولات (قيمة جهازية لا تُخزن PIN).
- **القرار:** LATER — خطر نظري على معرض بأجهزة معدودة؛ لا يسبق T1/T2.

## 4. دعم قابلية الاستخدام المتوقع

### U1 — جولة أول تشغيل داخل التطبيق (First-run orientation)
- **الدليل:** الإنتاج يبدأ فارغًا (invariant)، و`docs/OPERATIONS_GUIDE.md` خارج التطبيق؛ المشغلة غير التقنية تفتح نظامًا فارغًا بلا إرشاد. المنتجات المماثلة كلها تقدّم onboarding (CloudBridal يبيعه كميزة «done-for-you migration»).
- **أصغر نسخة:** بطاقة إرشاد على لوحة التحكم تظهر فقط عندما يكون النظام فارغًا فعلًا (لا قطع ولا عميلات): 3 خطوات مرتبة (أضيفي أول قطعة ← أضيفي عميلة ← أنشئي حجزًا تجريبيًا) مع رابط الدليل.
- **معايير القبول:** تختفي تلقائيًا بعد أول قطعة+عميلة؛ لا تظهر بعد استعادة نسخة؛ اختبار ui-contract.
- **قياس النجاح:** أول جلسة تشغيل حقيقية تكتمل بلا سؤال دعم «من أين أبدأ».
- **القرار:** LATER (قيمة عالية لكنها لا تسبق الثقة).

### U2 — بحث عام سريع (عميلة/قطعة/حجز من أي شاشة)
- **الدليل:** البحث موجود لكل صفحة على حدة (12 موضعًا ببحث عربي محسّن 13.01)؛ الانتقال بين ثلاث صفحات لسؤال هاتفي واحد كلفة كاونتر حقيقية وقت الذروة.
- **أصغر نسخة:** حقل في شريط التطبيق يفتح نتائج مجمعة (عميلات/قطع/حجوزات) تستدعي فهارس البحث القائمة.
- **القرار:** LATER — تحسين إنتاجية لا سد فجوة؛ يُقيّم بعد أسبوعين تشغيل حقيقي.

## 5. فرص تنافسية/نمو

### G1 — طلب موعد منظم من الصفحة العامة (بدل WhatsApp الخام)
- **الدليل:** `/landing` تعرض المتاح وتحوّل لـ WhatsApp فقط؛ الطلبات تصل بلا بنية (القطعة/التاريخ/المقاس) فتضيع دقائق كاونتر في الاستكمال. المقارنات (Booqable/CloudBridal) تجعل الحجز الذاتي واجهتهم الأولى.
- **أصغر نسخة:** نموذج «اطلبي موعدًا» (اسم/هاتف/تاريخ/قطعة اختيارية) يكتب سجل `appointments` بحالة «طلب وارد» + زر تأكيد يفتح WhatsApp جاهزًا.
- **المخاطر:** متوسطة — سطح عام جديد يكتب؛ يحتاج RPC مُساهِلًا ضيقًا وحارس spam بسيطًا (honeypot + حدًا لكل رقم هاتف).
- **القرار:** LATER مشروط — إن أثبتت المالكة أن >20% من محادثات WhatsApp تبدأ بلا بيانات كافية، يصبح NEXT. لا يُبنى الآن احترازًا من تضخيم مبكر.

### G2 — تقارير موسمية جاهزة (مناسبات/أعياد) ومقارنة سنوية
- **الدليل:** التقارير الحالية تشغيلية/مالية؛ لا رؤية «موسم العيد مقابل العام الماضي». قيمة تجارية حقيقية لكنها مبنية على بيانات لا تقل عن سنة.
- **القرار:** LATER طبيعي — يُبنى عندما توجد بيانات كافية؛ لا قيمة قبلها (overbuild الآن).

## 6. قدرات اختيارية يجب أن تنتظر

| القدرة | سبب الانتظار | إعادة النظر |
| --- | --- | --- |
| توقيع إلكتروني على العقد | العقد المطبوع + الصور يفيان قانونيًا/عمليًا الآن؛ e-sign يضيف اعتمادية وخدمة خارجية | بعد إثبات نزاعات واقعية متكررة |
| إرسال WhatsApp/SMS آلي (API) | تكلفة recurring + سياسات Meta + الأرقام العمانية؛ التسليم اليدوي يمنح مراجعة بشرية (قرار واعٍ في 13.08) | قرار مالك بتكلفة معلنة |
| روابط/مدفوعات إلكترونية | خارج نطاق المنتج مؤكدًا (README/ADR)؛ السوق نقد/تحويل/شبكة جهاز | فقط بقرار تجاري من المالكة |
| مزامنة تقويم خارجية (iCal/Google) | لا طلب موثق؛ التقويم الداخلي كافٍ لمعرض واحد | عند طلب فعلي متكرر |
| تصدير محاسبي (QuickBooks…) | CSV بترميز آمن يفي بالمحاسب الحالي | عند تعيين محاسب يطلب صيغة محددة |

## 7. ما يجب تبسيطه/دمجه/إخفاؤه/حذفه

| العنصر | الدليل | التوصية | الأثر/المخاطر |
| --- | --- | --- | --- |
| ~~`windows-release.yml`~~ | كان يبني Tauri من فرع `feature/supabase-auth` الميت | **نُفّذ 2026-08-20:** حُذف الملف؛ عُدّل شاهد Node-22 في `tests/runtime-env.test.mjs` ليغطي CI الحي فقط؛ `src-tauri` باقٍ للاسترجاع التاريخي | البوابة 710/710 بعد الحذف ✓ |
| تعريف 12.09 المتناقض | مسجّل DEFERRED بينما 11.07–11.09 أنجزت PIN ومؤشر السعة | تحرير النص ليعكس أن المتبقي هو «النسخ التلقائي المجدول خادميًا» فقط | وضوح الطابور؛ بلا مخاطرة |
| وثائق تاريخية متناقضة (BUSINESS_MODEL §6 «مشاكل حالية» معظمها مُصلح…) | `PROJECT_STATUS.md` §5 | نقل ملفات التدقيق التاريخية إلى `docs/archive/` مع لافتة «تاريخي» | يقلل ضباب القارئ الجديد؛ لا حذف للتاريخ |
| ~~`postcss.config.cjs` + `postcss.config.mjs`~~ | ملفان متطابقان لنفس الأداة | **نُفّذ 2026-08-20:** أُبقي `.mjs` الأصلي لحزمة type:module؛ البناء الناتج متطابق بايتًا ببايت (md5 واحد) | تبسيط بمخاطرة مثبتة الصفر |
| `src/services/*` delegates | مرحلة معمارية مخططة في `TARGET_CODE_ARCHITECTURE.md` | تركها لطور المعمارية — لا خلط مع عمل المنتج | تجنب «إعادة كتابة مقنّعة» |

## 8. التسلسل الموصى به (قرار المحلل، ليس قائمة أمنيات)

| المسار | البند | لماذا الآن/لاحقًا |
| --- | --- | --- |
| **Now (تُنفذ في هذه الجلسة)** | T1 نسخ الخادم اللحظية | أعلى خطر موثق، أقل كلفة، بنية جاهزة، آمن وقابل للعكس، معيار صناعي واضح |
| **Now (توثيق فقط)** | حذف `windows-release.yml` + تنظيف تناقض 12.09 + ترحيل أرشيف الوثائق | تبسيط صريح مقترح أعلاه — يُنفذ كدفعة توثيق صغيرة مستقلة عن T1 |
| **Next** | T2 حارس حجم اللقطة؛ ثم T3 دليل الحسابات + MFA؛ ثم جلسة أدلة الأجهزة (4.02/4.05…) بمشاركة المالكة | T2 يتشارك ملفات مع T1 (تعاقب لا تزامن)؛ T3 يحتاج حساب المالكة؛ الأجهزة تحتاج جهازًا فعليًا |
| **Later** | T4، T5، U1، U2، G2؛ وG1 عند تحقق الشرط الموثق | قيمة مؤجلة أو مشروطة بدليل تشغيلي |
| **Do Not Build** | SaaS متعدد المعارض/فروع؛ بوابة حجز ذاتية تحل محل WhatsApp؛ دفع إلكتروني؛ تكامل محاسبي كامل؛ أي AI/trend features | قرارات نطاق مؤكدة (ADR 0001/README) أو تكلفة صيانة بلا قيمة مثبتة أو مخالفة لطبيعة السوق |

### لماذا T1 قبل كل شيء؟
1. تصنيف الخطر: #1 في `PROJECT_STATUS.md` (استمرارية البيانات المركزية).
2. الاستعداد: البنية التحتية منشأة ومصانة بسياسات — الكود غير موصول فقط؛ لا migration جديدة إلزامية.
3. الانعكاسية: إزالة الإشارة تكفي للتراجع؛ لا حذف/تحويل بيانات.
4. الفشل لطيف: أفضل-جهد لا يعيق الإقفال اليومي — لا يمكن أن يضر رحلة تشغيل قائمة.

### معيار نجاح الخطة كاملة
- خلال 30 يوم تشغيل: 100% من أيام الإقفال لها نسخة خادم حديثة، وقائمة النسخ تحتفظ بآخر 20، وتمرين استعادة واحد ناجح موثق في `docs/RUNTIME_QA.md`.
- لا يُعلن أي بند «مكتملًا» دون الدليل الموصوف في قسمه.

## 9. مصادر المقارنة المجالسية

- Booqable / EZRentOut / Current RMS / Rentman (تصنيفات 2026): جرد، تقويم حجوزات، سجلات عملاء، باركود، تقارير، واجهة حجز + مدفوعات اونلاين — LENA يغطي التشغيلي منها ويستبعد الحجز/الدفع الذاتي بقرار مؤسسي.
- CloudBridal / TSE Bridal / StyleSavvy (أدلة شراء 2026): مواعيد وتذكيرات، قياسات وملابس fitting، تعديلات، ودائع وأقساط، بوابات عميلات وتوقيع إلكتروني — اعتمدت هذه الوثيقة منها فقط ما يخدم معرضًا واحدًا (تذكيرات/قياسات/مواعيد موجودة فعلًا) وأجّلت البوابة والتوقيع بدليل الطلب.
