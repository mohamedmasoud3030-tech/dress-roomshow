# دليل تركيب وتشغيل تطبيق LENA

## المتطلبات:

- Node.js 22.23.2 هو الإصدار الافتراضي الموثق حاليًا (المدى المدعوم: ^22.23.2 || ^24.0.0 — راجعي `engines` في `package.json` و`.nvmrc`)
- npm أو pnpm

## التركيب

1. `git clone https://github.com/mohamedmasoud3030-tech/lenadress.git`
2. `cd lenadress`
3. `npm ci`
4. `npm run dev` (للتطوير)
5. `npm run build` (للإنتاج)

## التشغيل

التطبيق يعمل على `localhost:5173`.
