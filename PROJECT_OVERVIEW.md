# خريطة مشروع LENA

> آخر تحقق مباشر: 2026-08-17 — الفرع `arena/01a00fc5-lenadress` عند الأساس `7930a9e`.
>
> المصطلحات: **متحقق** = ثبته الكود أو فحص نُفّذ؛ **استنتاج** = تفسير مدعوم دون تشغيل حي كامل؛ **غير محسوم** = يحتاج بيئة أو بيانات تشغيل حقيقية.

## 1. ما هو المنتج؟

**متحقق:** LENA نظام عربي باتجاه RTL لتشغيل **معرض واحد** لفساتين ومستلزمات المناسبات. سطح الإصدار الرسمي الحالي هو Web App قابل للتثبيت كـ PWA، مع Supabase كمصدر مركزي للحقيقة. يخدم دورة القطعة من إدخال المخزون إلى الحجز أو البيع، ثم التسليم والاسترجاع والفحص/الخدمة، ويربط ذلك بالعملاء والحركات المالية والإقفال والتقارير والتدقيق والنسخ الاحتياطي.

ليس المنتج SaaS متعدد المعارض، ولا يدعم فروعًا أو دفعًا إلكترونيًا. لا يوجد تطبيق عميل منفصل لإتمام الحجز؛ الصفحة العامة تعرض المتاح وتحول الاستفسار أو طلب الموعد إلى WhatsApp.

## 2. المستخدمون والأدوار

| المستخدم | الوصول المتحقق |
| --- | --- |
| زائرة عامة | `/landing` فقط: عرض القطع التي ينشرها الإسقاط العام `catalogue_items`، معلومات المعرض، واتصال/طلب موعد عبر WhatsApp. لا وصول لبيانات العملاء أو المال. |
| `staff` فعّال | تسجيل دخول Supabase، ثم PIN محلي للجهاز، ثم جميع الشاشات التشغيلية عدا `/preferences`. يستطيع تنفيذ أعمال المعرض. قيود الخادم تمنعه من تغيير مجموعات الإعدادات، حذف مجموعات تشغيل رئيسية، أو إزالة حركات مالية سابقة. |
| `admin` فعّال | صلاحيات الموظف، بالإضافة إلى `/preferences` وإدارة الإعدادات والنسخ والاستعادة والتصفير. سياسات PostgreSQL تسمح له أيضًا برؤية سجل `showroom_mutations` وأخطاء العميل. |
| حساب معطّل/بلا profile/جلسة غير صالحة | يفشل مغلقًا ويعود إلى `/login` مع حالة عربية مناسبة. |

**حد مهم:** لا توجد في الواجهة شاشة متحققة لإنشاء الحسابات أو تغيير دور مستخدم آخر. أول مستخدم تنشئه منظومة Auth يصبح `admin` عبر migration؛ المستخدمون اللاحقون `staff`. إدارة دورة الحساب خارج تسجيل الدخول/الخروج غير موثقة كرحلة مكتملة داخل التطبيق.

## 3. الرحلات الأساسية

### رحلة التأجير

1. إضافة قطعة أو اختيار قطعة قائمة، مع كود وباركود ثابتين.
2. إضافة عميلة أو اختيارها، مع منع تكرار الهاتف بصيغ عُمانية مكافئة.
3. البحث عن التوفر لفترة، مع احتساب تجهيز ما قبل التسليم وتنظيف ما بعد الإرجاع.
4. إنشاء حجز من قطعة واحدة أو عدة بنود، ويمكن إرفاق ملحقات.
5. تحصيل دفعة حجز/إيجار وتأمين مسترد كمعنيين ماليين منفصلين.
6. التسليم بعد تحقق بوابة الدفع، مع صور حالة اختيارية وملحقات.
7. الاسترجاع، حساب تأخير مقترح، تسجيل ضرر، وتسوية التأمين.
8. إرسال القطعة إلى `inspection` أو `laundry` أو `maintenance` أو `damaged`؛ لا تعود متاحة تلقائيًا.
9. إكمال مهمة الخدمة بحالة نهائية صريحة.
10. انعكاس العملية في المال والتقارير والإقفال والتدقيق والنسخة المركزية.

### رحلة البيع والمرتجع

- إنشاء فاتورة بيع لبند أو عدة بنود، طباعة الفاتورة، وتغيير حالة القطع إلى `sold` ذريًا.
- مرتجع كل بند مرة واحدة فقط، مع رد مالي وإرسال القطعة إلى الفحص بدل الإتاحة المباشرة.

### رحلة اليوم التشغيلي

- لوحة تحكم لعمليات اليوم والمتأخرات والأرصدة غير المحصلة وحركة النقد والتأمينات المستحقة.
- مواعيد، تذكيرات مشتقة، WhatsApp، وقائمة انتظار تعيد فحص التوفر.
- تسجيل مصروفات عامة أو مرتبطة بقطعة.
- إقفال اليومية ومقارنة النقد المتوقع بالفعلي. بعد الإقفال تُمنع حركات المال لذلك التاريخ حتى إعادة فتحه بسبب مسجل.
- بعد الإقفال يُطلب تنزيل نسخة JSON كاملة؛ فشل التنزيل لا يلغي الإقفال ويظهر زر إعادة المحاولة.

### رحلة الاستمرارية

- تصدير/استيراد نسخة احتياطية versioned تشمل المجموعات وصور IndexedDB.
- تحقق قبل الاستبدال، ومحاولة استعادة الحالة السابقة عند فشل الاستيراد.
- PIN من 6 أرقام خاص بالجهاز، مخزن كـ PBKDF2 verifier مع salt وليس كرقم صريح.
- PWA يعرض تحديثًا جديدًا ويترك للمشغلة اختيار وقت إعادة التحميل.

## 4. جرد المسارات الكامل

| المسار | الحماية | الوظيفة |
| --- | --- | --- |
| `/landing` | عام | صفحة المعرض: الهوية، الخدمات، التصنيفات، المعروض المتاح، الأسئلة، التواصل وWhatsApp. |
| `/login` | عام | دخول البريد وكلمة المرور عبر Supabase Auth. |
| `/` | Auth + cloud hydration + PIN | لوحة التحكم والتنبيهات وإقفال اليوم. |
| `/inventory` | الحماية التشغيلية | المخزون؛ بحث وتصفية، grid/list، تجميع حسب التصميم، إضافة، أكواد وباركود وطباعة دفعات. |
| `/inventory/:code` | الحماية التشغيلية | تفاصيل القطعة، الصور، lifecycle، البيع/الأرشفة/الحذف المشروط، الباركود وربط التصميم. |
| `/designs/:code` | الحماية التشغيلية | تفاصيل التصميم، القطع/المقاسات/الألوان، إضافة variants، والتوفر للفترة. |
| `/availability` | الحماية التشغيلية | بحث عكسي عن القطع والملحقات المتاحة، أسباب التعارض، أقرب فترة وبدائل. |
| `/accessories` | الحماية التشغيلية | كتالوج الملحقات، الحالات، الأكواد والباركود والملصقات. |
| `/customers` | الحماية التشغيلية | العملاء، الأرشفة، القياسات، ملاءمة المقاس، السلوك والملاحظات. |
| `/reservations` | الحماية التشغيلية | قائمة وتقويم شهر/أسبوع/يوم، إنشاء متعدد البنود، إلغاء، عقد مطبوع، CSV. |
| `/appointments` | الحماية التشغيلية | مواعيد الزيارات وحالاتها. |
| `/delivery-return` | الحماية التشغيلية | طابور التسليم والاسترجاع، الملحقات، صور الحالة، الرسوم وتسوية التأمين. |
| `/sales` | الحماية التشغيلية | فواتير المبيعات والطباعة ومرتجعات البنود. |
| `/service` | الحماية التشغيلية | مهام الفحص والغسيل والصيانة/الخياطة والضرر، والتكلفة المرتبطة. |
| `/stocktake` | الحماية التشغيلية | جلسات الجرد والمسح والنتيجة دون تغيير حالة القطع تلقائيًا. |
| `/payments` | الحماية التشغيلية | دفتر التحصيلات والدفعات والتأمينات والرسوم والاستردادات؛ بحث وCSV. |
| `/expenses` | الحماية التشغيلية | المصروفات، طرق الدفع، الربط بقطعة، بحث وCSV. |
| `/daily-closing` | الحماية التشغيلية | إقفال/إعادة فتح يومية النقد، الفروقات، والنسخة التالية للإقفال. |
| `/audit-log` | الحماية التشغيلية | سجل التدقيق والبحث وCSV. |
| `/reminders` | الحماية التشغيلية | تذكيرات الاستلام والإرجاع والتأخير والأرصدة، نسخ/فتح WhatsApp وتجاهل يومي. |
| `/waitlist` | الحماية التشغيلية | قائمة انتظار مرتبة، فرص التوفر، إشعار/تحويل/إغلاق. |
| `/reports` | الحماية التشغيلية | ملخصات المال والعملاء والحجوزات والمخزون والتشغيل. |
| `/inventory-performance` | الحماية التشغيلية | ربحية واستخدام وأداء القطع والتصاميم، trend، CSV وطباعة. |
| `/preferences` | `admin` فقط | نسخ/استعادة/تصفير، قواعد التشغيل، التأخير، PIN، قوالب الرسائل، الطباعة، هوية المعرض والإصدار. |
| أي مسار آخر داخل shell | الحماية التشغيلية | صفحة 404. |

## 5. جرد القدرات

### المخزون والتصاميم

- فساتين وملحقات وحقائب وأحذية وطرح وأنواع أخرى.
- هوية داخلية ثابتة، كود/باركود فريد متزايد لا يعاد استخدامه، وسجل retired codes.
- rent/sale flags، سعر شراء وإيجار وبيع وتأمين مقترح، صور وملاحظات.
- حالات فعلية: `available`, `rented`, `inspection`, `laundry`, `maintenance`, `damaged`, `sold`, `inactive`، مع توافق legacy محدود.
- تصميم يجمع قطعًا مستقلة بحسب المقاس واللون؛ التوفر يبقى على مستوى القطعة.
- ضغط الصور إلى WebP/JPEG، ورفع صور الكتالوج إلى Supabase Storage عند المسار المدعوم.
- مسح كاميرا عبر ZXing مع إدخال يدوي بديل في الكود، لكن الجهاز الحقيقي غير متحقق.

### العملاء والتواصل

- بيانات اتصال، قياسات منظمة ونص legacy، اقتراح مقاس وملاءمة قطعة.
- حالات trusted/normal/warning/blocked، سلوك مشتق من التأخير والضرر والإلغاء/no-show، وملاحظات منسوبة للمشغلة.
- WhatsApp deep link فقط؛ الرسالة تراجعها المشغلة وترسلها يدويًا. لا WhatsApp Business API ولا إرسال تلقائي.
- لا email transaction service؛ البريد مجرد معلومة/رابط `mailto:`.

### الحجز والتشغيل

- حجوزات متعددة البنود، snapshots تاريخية مع IDs ثابتة، ملحقات، calendar، rescheduling service، conflict engine موحد، عقود مطبوعة.
- صور حالة حتى أربع صور في مرحلة التسليم/الاسترجاع.
- late fee ثابت أو نسبة، grace وcap، ويظل اقتراحًا قابلًا للتعديل/الإلغاء.
- service queue، appointments، reminders، waitlist، stocktake.

### المال والرقابة

- ledger append-only منطقيًا للدفعات والمصروفات والمبيعات والإرجاعات.
- فصل دفعة الحجز عن التأمين المسترد؛ التأمين التزام وليس إيرادًا.
- idempotency وrollback محلي ثم commit واحد revisioned إلى الخادم.
- تقارير نقد وإيراد ومصروف وتأمينات وأرصدة وربحية/استخدام.
- إقفال يومي يمنع المال backdated حتى إعادة الفتح.
- CSV بترميز BOM وحماية formula injection، وطباعة عربية داخل overlay.

## 6. الواجهة وتجربة الاستخدام

- React 18 + TypeScript + React Router 7؛ تحميل كسول لجميع صفحات التشغيل تقريبًا.
- Tailwind CSS و`Noto Sans Arabic` محلي، `html lang="ar" dir="rtl"`.
- mobile-first: شريط سفلي لأربع رحلات يومية، قائمة "المزيد"، sidebar لسطح المكتب، cards بدل الجداول العريضة، حد أدنى 320px.
- قواعد تمنع horizontal overflow وiOS field auto-zoom، وتستخدم safe-area و`visualViewport` في modal.
- أنماط focus ظاهرة، labels و`aria-*` وlive regions، focus trap داخل modal، وعدم قفل browser zoom.
- forms ليست موحدة بالكامل: أربع نماذج رئيسية تستخدم `react-hook-form` + Zod (`inventory`, `accessories`, `customers`, `reservations`)؛ نماذج أخرى تستخدم state وHTML constraints وتحققًا داخل service.
- لا نظام localization متعدد اللغات؛ العربية نصوص مباشرة في الواجهة مع format `ar-OM`. RTL متحقق، لكن الترجمة للغات أخرى غير موجودة.

## 7. ما ليس موجودًا

- لا تعدد معارض/tenants أو فروع؛ كل الحسابات الفعالة تشارك snapshot واحدًا `id='main'`.
- لا دفع إلكتروني، بوابة بطاقات، خرائط، AI، analytics تسويقي، queues أو webhooks أو cron jobs.
- لا إرسال WhatsApp آلي أو email آلي.
- لا offline writes أو conflict merge؛ offline يحمّل shell فقط، والتشغيل يحتاج الشبكة.
- لا تطبيق Tauri مدعوم رسميًا رغم بقاء الكود التاريخي.
